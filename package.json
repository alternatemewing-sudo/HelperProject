{
  "name": "math-solver-potato",
  "version": "1.0.0",
  "description": "Math-solver — a simple potato redirector (tokenize links, panic button)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
